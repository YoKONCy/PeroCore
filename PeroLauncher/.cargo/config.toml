[target.x86_64-pc-windows-msvc]
# 使用 lld 提高链接速度，且对增量编译支持良好
rustflags = ["-C", "link-arg=-fuse-ld=lld"]

[profile.dev]
# 核心优化：0 级调试信息（不生成 PDB），这是解决 Windows 链接慢的根本
debug = 0 
# 确保开启增量编译
incremental = true
# 移除 strip，因为它会破坏增量编译的指纹校验
# strip = "debuginfo" 
