{
  "id": "anime-finder",
  "name": "番剧识别",
  "version": "1.0.1",
  "author": "PeroCore",
  "description": "通过截图识别番剧出处。使用 trace.moe API 进行以图搜番。",
  "type": "tool",
  "entry": "index.ts",
  "category": "community",
  "platforms": ["windows", "linux", "darwin", "docker"],

  "toolDefinition": {
    "name": "find_anime_by_image",
    "description": "识图搜番。通过截图或图片链接查找动漫出处，返回番名、集数和时间点。",
    "parameters": {
      "type": "object",
      "properties": {
        "image_url": {
          "type": "string",
          "description": "图片的 URL 地址"
        }
      },
      "required": ["image_url"]
    }
  }
}
