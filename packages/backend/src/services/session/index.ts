/**
 * Session 模块导出（AIOS: 已废弃）
 *
 * SessionService 已移除，被以下服务替代：
 * - CompanionSchedulerService（陪伴调度管理）
 * - ThreadService（会话持久化）
 * - RuntimeStateService（运行时状态管理）
 *
 * @module packages/backend/src/services/session
 */
// AIOS: SessionService 已移除，此模块保留为空，待后续清理
