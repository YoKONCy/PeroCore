{
  "toolDefinition": {
    "name": "interact_with_social",
    "description": "让正在运行的社交Agent读取指定群聊上下文，并根据给定意图自主判断是否参与本次聊天。",
    "display": {
      "label": "联系社交助手",
      "description": "让社交助手查看指定群聊并参与互动。",
      "icon": "chat",
      "color": "pink",
      "style": "social"
    },
    "parameters": {
      "type": "object",
      "properties": {
        "group_id": { "type": "string", "description": "目标群聊ID" },
        "intent": { "type": "string", "description": "本次参与意图、希望关注的内容和行为边界" }
      },
      "required": ["group_id"],
      "additionalProperties": false
    }
  }
}
