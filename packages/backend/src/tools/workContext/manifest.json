{
  "id": "work-context",
  "name": "workContext",
  "displayName": "工作上下文维护",
  "version": "1.0.0",
  "description": "维护当前会话中由角色主动总结的工作上下文。",
  "type": "tool",
  "entry": "index.ts",
  "category": "system",
  "platforms": ["windows", "linux", "darwin", "docker"],
  "registration": "static",
  "tools": [
    {
      "name": "manage_work_context",
      "description": "工作上下文只由系统按轮保存文件读取、代码搜索和网页读取获得的文本正文及必要来源信息；不会保存 ReAct 叙述、终端输出、扩展日志、状态回执、目录或文件元数据及原始调用参数。update 用于将当前有效内容手动压缩为完整摘要并重新计算有效期；clear 清空全部内容。没有压缩或清空需求时不要调用。",
      "parameters": {
        "type": "object",
        "properties": {
          "action": {
            "type": "string",
            "enum": ["update", "clear"],
            "description": "update 整体覆盖工作上下文；clear 一键清空。"
          },
          "content": {
            "type": "string",
            "description": "update 时必填：合并新旧信息、自我总结并压缩后的完整工作上下文，最多 8000 字。"
          }
        },
        "required": ["action"],
        "additionalProperties": false
      },
      "display": {
        "label": "维护工作上下文",
        "description": "整理或清空最近几轮仍需使用的临时信息。",
        "icon": "thought",
        "color": "sky",
        "style": "system"
      }
    }
  ]
}
